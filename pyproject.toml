[project]
name = "c3r-evals"
version = "0.1.0"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}

[project.optional-dependencies]
parquet = ["pyarrow==21.0.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
